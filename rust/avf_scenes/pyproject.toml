[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[project]
name = "avf_scenes"
description = "PyO3/Rust scene-detection frame-differencing extension for auto-video-fixer (docs/REQUIREMENTS.md R5.1)"
requires-python = ">=3.14"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
]
dynamic = ["version"]

[tool.maturin]
module-name = "avf_scenes"
